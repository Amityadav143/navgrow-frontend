# Shared defaults for all modes. Mode files (.env.development / .env.production)
# and machine-local files (.env.local) override these — see Vite env precedence.
# API base is intentionally NOT set here: the code defaults to same-origin '/api'
# in production and http://localhost:8080/api in dev.
VITE_RAZORPAY_KEY_ID=rzp_test_1DP5mmOlF5G5ag
